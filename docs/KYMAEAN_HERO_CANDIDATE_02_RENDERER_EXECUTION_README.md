# Kymaean Hero Candidate 02 — Renderer Execution

Status: DIRECTOR AUTHORIZED / RENDERER ISOLATION REQUIRED

Run exactly three fresh regular non-project renderer conversations:

- G1 — Residual Presence
- G2 — Shared Pressure Field
- G3 — Translated Identity

For each conversation:
1. Paste only that renderer packet.
2. Generate normally one image.
3. Do not show historical Stage/O0 images to the renderer.
4. Do not show another candidate to the renderer.
5. Do not correct or refine the first output by default.
6. Return the raw output to the Ensemble/Kymaean Director project context.

After all three exist:
- archive raw outputs first;
- evaluate each independently;
- test the 4:5 crop and left desktop identity zone;
- apply G4 Occluded Chorus as an evaluation pressure;
- compare only after independent evaluation;
- stop at Director review.

Do not generate a fourth candidate, merge candidates, or refine a favorite by default.
