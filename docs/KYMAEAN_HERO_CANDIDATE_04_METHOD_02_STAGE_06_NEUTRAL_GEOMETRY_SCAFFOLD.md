# KYMAEAN — HERO CANDIDATE 04 METHOD 02 STAGE 06 NEUTRAL GEOMETRY SCAFFOLD

Status: **CONSTRUCTED / RECURSIVELY AUDITED / DIRECTOR REVIEW REQUIRED**
Date: 2026-09-07

## Authority

Stage 06 is the Director-authorized continuation of approved Candidate 04 construction `C04-ACLT-01`.

Authorized here:

- one deterministic candidate-specific Neutral Geometry Scaffold;
- geometry-only representation of the approved Stages 1–5 construction;
- recursive audit.

Not authorized:

- reference transfer;
- sterile renderer packet;
- image generation;
- palette, material, lighting, style or final artwork decisions;
- website/app implementation.

## Canonical scaffold artifacts

- `docs/evidence/scaffolds/C04-ACLT-01_STAGE06_NEUTRAL_GEOMETRY_SCAFFOLD.svg`
- `docs/evidence/scaffolds/C04-ACLT-01_STAGE06_NEUTRAL_GEOMETRY_SCAFFOLD_MANIFEST.json`

The SVG is the one visible structural scaffold. Its `scaffold` group is the only structural-export layer. `audit-guides` is hidden by default and exists only to preserve deterministic test geometry. No renderer transfer is authorized.

## Frozen geometry

Artboard: **1536 × 960**.

Responsive constraints:

- desktop source window: x `0–1536`, y `48–912`;
- mobile source window: x `554–1321`, y `0–960`;
- complete semantic core: x `620–1300`, y `190–850`;
- desktop calm region: x `0–600`, y `48–912`.

Critical points:

- A shoulder/forearm root `(891,403)`;
- B changed-state shoulder/forearm root `(1106,403)`;
- B baseline-restored root `(1275,547)`;
- contact locus `(998,407)`;
- A passive-load point `(826,700)`;
- B changed support point `(1112,510)`.

Counterfactual reach constants:

- `R_A = 132`;
- `R_B = 138`;
- positive margin `m = 35`;
- changed root distance `215`;
- baseline-restored root distance `410.112`.

Therefore:

`215 <= 132 + 138 - 35 = 235` — PASS.

`410.112 >= 132 + 138 + 35 = 305` — PASS.

The field is encoded by three continuous longitudinal paths: upper boundary, lower boundary and secondary contour. Each returns to aligned baseline witnesses on both sides of the local distributed down/up flexure.

A and B use different figure-scale/body-axis geometries. Their forearm regions overlap only at the changed-state contact locus; no symbolic connector survives in the scaffold.

## Stage 06 disposition

The deterministic scaffold now exists and has been recursively audited against semantic dependency, baseline/change recoverability, counterfactual necessity, two-person necessity, responsive survival and forbidden aesthetic leakage.

Exact next gate:

**APPROVE / CORRECT / REJECT — Candidate 04 Stage 06 Neutral Geometry Scaffold.**

Approval would authorize Stage 07 sterile artwork translation/pre-render package construction only. It would not authorize scaffold transfer to a renderer or image generation.
