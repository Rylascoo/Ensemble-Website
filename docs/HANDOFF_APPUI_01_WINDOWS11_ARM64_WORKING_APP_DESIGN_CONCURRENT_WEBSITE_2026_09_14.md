<!-- D-R1-STATUS: ACTIVE LAW -->

# Ensemble / Kymaean - APPUI-01 Fresh-Chat Handoff with Concurrent Website Sol

Status: `PHASE C ACTIVE / APPUI-01 ITERATIVE SCREEN-FIRST / WINDOWS 11 ARM64 / CONCURRENT WEBSITE STREAM`
Date: 2026-09-14

Recover live authority from GitHub first. This handoff is a recovery aid, not authority. At coordination capture, APPUI branch `design/phase-c-completion-audit-2026-09-13` was `5ec8d6de728f588b861eccadbd8ff7019aa63fdc`, Website `main` was `0ebd9ce278847269208ddf43fc482e0b1ca7165d`, and Engineering `main` was `57ec145c09e701e5d62907ae75046f38b5cbe285`. Verify rather than assume.

## Required recovery order
1. Resolve live refs for `Rylascoo/Ensemble-Website`, including `main`, the APPUI serial branch named by current authority, and current `site/*` / other unique concurrent branches.
2. Read `AGENTS.md` through EOF.
3. Read `CURRENT_STATE.md` from the exact APPUI active ref, not default `main` by habit.
4. Read `docs/evidence/APPUI_01_CONCURRENT_WEBSITE_SOL_COORDINATION_2026_09_14.json` (SHA-256 `0e520b51b80065a5fbd74288946b42edee87917b2cc8538ad847f937310d38a1`).
5. Read `docs/evidence/APP_UI_DIRECTOR_ITERATIVE_DESIGN_PIVOT_2026_09_14.json` and the historical app inputs named below.
6. Fresh-read `Rylascoo/Ensemble-Project/CURRENT_STATE.md` when product/Engineering semantics matter.

## Concurrent repository reality
A separate **Website Sol** is actively working on the website/publication portion of this same `Ensemble-Website` repository. Website movement on `main` or `site/*` is expected and is not APPUI drift. APPUI-01 and Website Sol must remain separate workstreams unless an actual cross-stream reconciliation is needed. Do not merge/rebase/reset/delete/overwrite Website Sol work merely to synchronize refs. Preserve every branch with unique commits.

APPUI owns app visual-design work and its isolated worktree/branch. Website Sol owns public-site/Cloudflare/publication work. If shared continuity files later conflict, reconcile them as a factual union after fresh-ref comparison; never silently discard either stream.

## Director correction / active design mode
The product-design target is a **Microsoft Windows 11 desktop app on ARM64 intended for Microsoft Store distribution**. Do not design it as a phone/mobile app. Human artwork is not required. CHARART-01 is terminated with no master adopted. The design process is intentionally screen-first and iterative: make useful desktop screens now and refine visual identity through actual compositions and Director reaction.

`APPUI-01 - Windows 11 ARM64 Working App Visual Prototype` is active. It is not a frozen-candidate gate. Provisional color, type, material, state and motion choices may be explored as working design choices without silently promoting final authority.

## First useful surface set
- Production / Scene overview;
- live Stage workspace;
- selected Character / state inspector;
- Performance / causal-history strip or adjacent history surface.

Historical inputs: `docs/KYMAEAN_APP_INFORMATION_RELATIONSHIP_ARCHITECTURE_PROPOSAL_01.md`, `docs/KYMAEAN_APP_SHELL_NAVIGATION_GRAMMAR_PROPOSAL_01.md`, `docs/KYMAEAN_APP_TASK_FIRST_LOW_FIDELITY_INTERACTION_PROTOTYPE_01.md`, and `docs/evidence/APP_SYN_01_DESIGN_SOL_WHOLE_SYSTEM_VIEW_01.json`. Use them as inputs, not gates to replay.

## Preserved constraints
`PKT-STAGE-CORE-02` remains exact Stage authority. CPS-B remains canonical Character presence, but portraits are optional. APPICON E06 remains the design-master app-icon envelope around exact C0. DUR-01 remains PASS evidence. CLR F2/F1 remains unselected, so mockups may explore color provisionally without claiming a final palette. Preserve accessibility intent, keyboard/focus/state distinction, reduced-motion/static truth, privacy/perspective boundaries, and truthful product semantics.

## Platform / authority boundary
Design for a resizable Windows 11 desktop experience with keyboard/mouse first-class and touch compatibility where natural. Native WinUI/XAML implementation, Windows runtime accessibility validation, ARM64 performance, MSIX/package identity, WACK/Store certification, Partner Center and shipping remain Engineering/outside-lane authority.

## Exact next action
Create one or two substantial full-window Windows desktop APPUI-01 compositions that expose hierarchy, density, navigation, Stage adjacency, Character presence, inspector behavior and history placement. Iterate from Director reaction. Do not resume human-art research, do not restart abstract convergence gates by default, and do not take over Website Sol's concurrent website work.
