# KYMAEAN — HERO CANDIDATE 04 METHOD 02 STAGE 07 STERILE ARTWORK TRANSLATION / PRE-RENDER PACKAGE

Status: **CONSTRUCTION COMPLETE / DIRECTOR REVIEW REQUIRED / NO REFERENCE TRANSFER OR IMAGE GENERATION AUTHORIZED**  
Date: 2026-09-07
Candidate: `C04-ACLT-01`

## 1. Purpose

Translate the approved Stage 06 relational scaffold into the minimum renderer-facing instruction set needed for one later isolated artwork attempt, without re-delegating semantic causality to the renderer and without leaking project memory, scoring law, prior images or brand assets.

This package does not execute the renderer.

## 2. Approved structural input

Canonical scaffold:

`docs/evidence/scaffolds/C04-ACLT-01_STAGE06_NEUTRAL_GEOMETRY_SCAFFOLD.svg`

Only the visible `g#scaffold` structural layer is eligible for a later separately authorized transfer. The hidden `g#audit-guides`, metadata, title/description, labels and audit geometry are not transferable.

Transferred variable, if later authorized: **relational composition only**.

No aesthetic attribute is intended to transfer.

## 3. Renderer-context isolation

A later renderer context may receive only:

1. one sanitized structural scaffold derived from `g#scaffold` only;
2. `docs/evidence/renderer/C04-ACLT-01_STAGE07_STERILE_RENDERER_PACKET.txt`.

It must not receive:

- project/candidate names;
- brand marks, wordmarks or palette files;
- prior hero candidates or round images;
- scoring rubrics, thresholds or critique history;
- repository/Drive history;
- audit guides or coordinate annotations;
- engineering/product implementation detail.

No user-set reference-strength control is assumed. If the execution interface exposes one later, its exact fixed value must be added to this package before execution. If no such control is exposed, none is invented.

Visible diagram/scaffold artifacts in returned artwork = candidate failure, not a free reroll.

## 4. Translation controls

The sterile packet preserves:

- two visibly specific adults as the primary meaningful event;
- A as passive bodily load, never operator/helper;
- B as altered-position occupant, never independently supported;
- one continuous broad changed field with recoverable baseline on both sides;
- visible distributed coupling, not hinge/pivot/device logic;
- one neutral forearm-to-forearm human relation enabled by B's changed position;
- quiet, materially credible, non-spectacular visual behavior;
- person distinctness through silhouette/body proportion, head/hair silhouette and outer-garment silhouette rather than color coding or labels;
- no private-state interpretation.

Candidate-specific aesthetic restraint is intentionally bounded rather than finalized: natural physical coherence, subdued material behavior, human-readable form, no fantastical glow, no interface/diagram language, no named art style and no fixed brand palette.

## 5. Responsive control

The later artwork must preserve the scaffold's complete semantic core inside source x `620–1300`, y `190–850`; both baseline witnesses remain inside the fixed mobile source window x `554–1321`, y `0–960`.

The left desktop region x `0–600` remains low-information and nonessential to the causal read.

No crop rescue or Harness change is allowed.

## 6. Material-difference declaration

Relative to completed Method 01 rounds, the deliberate method difference is:

**a deterministic geometry scaffold may carry relational composition into the isolated renderer instead of asking prose alone to invent it.**

Unchanged:

- renderer isolation;
- no prior candidate-image inheritance;
- frozen post-render criteria;
- fixed Harness geometry;
- pixels remain binding;
- no post-hoc rescue.

No brand/reference artwork is added.

## 7. Proposed execution accounting

If separately authorized:

- experiment label: `METHOD02-R04-C04`;
- renderer requests: **1**;
- images requested: **1**;
- isolated renderer context: **1**;
- free rerolls: **0**;
- transferred structural reference: exactly one sanitized Stage 06 scaffold;
- renderer text: exactly the frozen sterile packet in this package.

Any returned image consumes the attempt regardless of pass/fail. A tool failure that returns no artwork stops execution and is reported; it does not silently authorize another request. Any additional request requires a new Director decision.

## 8. Stop consequence

After the single later authorized attempt, stop and evaluate the returned pixels under the already-frozen Hero Brief 02 / Falsification Criteria / Surface Escape / Harness law. Do not rewrite the packet, scaffold or criteria in response to the image before that evaluation is closed.

## 9. Current gate

**APPROVE / CORRECT / REJECT — Stage 07 sterile artwork translation / pre-render package.**

Approval of this package alone does not execute reference transfer or image generation. Renderer execution requires a separate explicit Director instruction.
