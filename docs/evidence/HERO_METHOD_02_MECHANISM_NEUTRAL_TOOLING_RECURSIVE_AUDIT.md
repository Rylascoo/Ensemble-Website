<!-- D-R1-STATUS: HISTORICAL EVIDENCE -->

# HERO METHOD 02 — MECHANISM-NEUTRAL TOOLING RECURSIVE AUDIT

Status: **COMPLETE / CLEAN FINAL PASS / TOOLING READY / NO CANDIDATE OR RENDER AUTHORIZED**  
Date: 2026-09-06

## Audited authority

- `docs/KYMAEAN_HERO_SUCCESSOR_DESIGN_CONSTRUCTION_METHOD_02.md`;
- `docs/evidence/HERO_SUCCESSOR_DESIGN_CONSTRUCTION_METHOD_02_RECURSIVE_AUDIT.md`;
- `docs/evidence/HERO_SUCCESSOR_DESIGN_CONSTRUCTION_METHOD_02_DIRECTOR_APPROVAL.md`;
- Hero Artwork Brief 02;
- frozen Falsification Criteria 01;
- frozen Surface Escape;
- corrected/re-frozen Harness 01;
- completed three-round exhaustion evidence and Director Option A decision.

## Audited tooling

- `docs/templates/HERO_METHOD_02_CONSTRUCTION_RECORD_TEMPLATE.md`;
- `docs/templates/HERO_METHOD_02_TWO_PERSON_NECESSITY_MATRIX_TEMPLATE.md`;
- `docs/templates/HERO_METHOD_02_RESPONSIVE_SEMANTIC_ENVELOPE_TEMPLATE.md`;
- `docs/templates/HERO_METHOD_02_CONSTRUCTION_REVIEW_PACKAGE_TEMPLATE.md`;
- `docs/KYMAEAN_HERO_METHOD_02_NEUTRAL_GEOMETRY_SCAFFOLD_FORMAT_01.md`;
- `prototypes/lane-b/hero-method-02-responsive-envelope-tool.html`.

## Pass 1 — authorization and scope

**PASS.**

All created artifacts are mechanism-neutral tools. None selects a Candidate 04 mechanism, human relationship, setting, pose, material substance, palette, style or renderer wording.

No candidate-specific scaffold exists. No reference transfer, renderer packet, Round 04 or image generation is authorized.

## Pass 2 — frozen-contract preservation

**PASS.**

The tools operate as conservative **pre-render construction admission controls** only. They do not create new post-render scoring rules or relax/reinterpret:

- Hero Brief 02 hard floors;
- Falsification Criteria 01;
- Surface Escape;
- Harness 01;
- no-compensating-average law;
- final-pixels authority.

Each template explicitly preserves this distinction.

## Pass 3 — semantic dependency / two-person necessity

**PASS.**

The construction-record and two-person matrix tools require one irreducibly shared proposition rather than two independent examples of a mechanism.

The matrix independently tests:

1. Person A removal;
2. Person B removal;
3. baseline restoration;
4. genericization/interchangeability.

Human Necessity remains distinct from S1: a pair may be individually necessary while an environmental mechanism still dominates surface ownership, and the tooling records that risk separately.

No private-state semantic burden is introduced.

## Pass 4 — changed-state reconstruction

**PASS.**

The construction record preserves Method 02's corrected minimum of **two independent visible geometric correspondences**.

It also rejects single-clue dependence and the failure mode where the changed shape merely looks intentionally designed.

This directly addresses Round 03 without converting that observation into a new frozen scoring category.

## Pass 5 — counterfactual necessity

**PASS.**

The Counterfactual Ledger requires:

- exact changed geometry;
- exact relation using it;
- baseline-restoration consequence;
- explicit enumeration of plausible alternate support/path/contact explanations;
- rejection of convenience-only changes.

This preserves the successful causal lesson from Round 03 without freezing gravity support or any other mechanism class.

## Pass 6 — responsive semantic envelope

**PASS after one correction.**

The template uses the unchanged Harness 01 dimensions and positions and requires all core evidence to survive the fixed mobile window. It preserves the Method 02 redundant-baseline-witness requirement.

The interactive envelope tool implements CSS `object-fit: cover` source-window calculation using:

`scale = max(containerWidth/sourceWidth, containerHeight/sourceHeight)`

and converts overflow under the frozen `object-position` values back into normalized source coordinates.

Independent arithmetic checks were run against multiple source aspect ratios, including the prior Round 02 and Round 03 source dimensions. The resulting mobile windows correctly concentrate toward the source center-right under `72% 50%`.

Correction applied during audit:

- the first tool visualization used a square SVG viewBox regardless of declared source aspect ratio; although normalized crop math was correct, the drawing could visually distort the source artboard. The tool was corrected so the visualization itself now preserves the declared source aspect ratio.

The tool does not move or optimize any frozen crop.

## Pass 7 — neutral scaffold format

**PASS after one correction.**

The scaffold format permits only relational/structural primitives and explicitly excludes palette, material identity, lighting, texture, facial/identity coding, historical style, brand geometry, private-state semantics and decorative art direction.

Correction applied during audit:

- the first draft risked turning example structural relations such as `support` or `reach` into a quasi-enum requiring later method revision for new relation classes. That would have weakened mechanism neutrality. The format now treats `relation` as an open structural field that must trace to an already-authorized construction record; examples gain no inheritance rights.

No candidate geometry is present in the format examples: zero coordinates and empty arrays are schema placeholders only.

## Pass 8 — reference-transfer containment

**PASS.**

The scaffold format clearly states that Method 02 approval does not authorize renderer transfer. A later execution package must separately approve exact scaffold bytes, transfer mechanism, geometry-only intent, any reference-strength control, leakage detection and output disposition.

Visible scaffold/diagram leakage remains candidate failure rather than a free reroll.

## Pass 9 — package completeness / stage ordering

**PASS after one worthwhile addition.**

The Method 02 proposal defines an eleven-part construction review set. A reusable Construction Review Package Template was added so future work cannot silently skip a stage or bundle construction approval with renderer execution.

The package template preserves separate gates for:

1. specific visual/mechanism direction;
2. candidate construction/package review;
3. renderer/reference-transfer execution;
4. post-return provenance/evaluation;
5. final hero selection.

## Pass 10 — simplicity / duplication

**PASS.**

The tooling is intentionally small:

- one combined semantic/baseline/counterfactual record;
- one specialized two-person matrix;
- one responsive-envelope record;
- one review-package assembly checklist;
- one neutral-scaffold format;
- one deterministic crop-window calculator.

Splitting every Method 02 stage into a separate form would add document overhead without stronger control. Collapsing the current set further would hide independently important failure modes.

No worthwhile simplification remained.

## Pass 11 — authority-recursion check

**PASS.**

None of the corrections made during this audit:

- selects a visual direction;
- begins Candidate 04;
- creates a candidate scaffold;
- alters a frozen hero requirement;
- modifies Harness 01;
- authorizes a renderer;
- creates a fourth-round accounting assumption.

The completed Rounds 01–03 remain closed historical evidence under Method 01.

## Final recursive pass

A complete final pass found no remaining material inconsistency, hidden candidate selection, frozen-authority drift, post-hoc scoring invention, reference-transfer loophole, crop-rescue path, mechanism inheritance, unnecessary complexity or worthwhile improvement within the approved tooling scope.

Final result:

**METHOD 02 MECHANISM-NEUTRAL TOOLING: COMPLETE / COHERENT / AUDITABLE / READY FOR USE AFTER A LATER SPECIFIC-DIRECTION AUTHORIZATION.**

## Current boundary

Method 02 = Director approved.

Mechanism-neutral tooling = complete / recursively audited.

Specific visual/mechanism direction = **NOT AUTHORIZED**.

Candidate 04 construction = **NOT AUTHORIZED**.

Candidate-specific neutral scaffold = **NOT AUTHORIZED**.

Reference transfer = **NOT AUTHORIZED**.

Renderer packet / Round 04 / image generation = **NOT AUTHORIZED**.

The next gate is therefore a **Director visual/mechanism-direction decision before any Candidate 04 construction begins**.
