KYMÆAN — HERO CANDIDATE 08 METHOD 02 STAGE 06 EDGE-CLEARANCE METADATA CORRECTION 01

Status: FINAL ARITHMETIC / METADATA CORRECTION / NO VISIBLE GEOMETRY CHANGE / ROUND 08 RESULT UNAFFECTED
Date: 2026-09-08
Candidate: C08-EWTF-01 — EDGE-WITNESSED PROPAGATED TORSIONAL FLEXURE / SEPARATED-HEAD SCAPULAR BRACE

PURPOSE

Reconcile the Candidate 08 Stage 6 support-root/free-edge metric against the exact integer source geometry. This correction changes no approved Stage 6 x/y geometry, Stage 6M geometry, renderer-visible reference, sterile packet, frozen criterion, Round 08 pixels or Round 08 evaluation.

DEFECT HISTORY

The original Stage 6 evidence carried `120 px`, calculated from the undeformed baseline foreground edge y=824 against the changed left support-root center y=704.

A first correction correctly identified the changed free-edge path as controlling, but published `102.8395582822086 px` after an erroneous normalized-to-pixel conversion. That number is withdrawn.

BINDING METRIC

The registered anti-edge metric is support-root/center to the foreground free edge at the same source-space x coordinate.

Canonical Stage 6 integer SVG:
- controlling changed support root = (1105,704);
- bracketing `field-free-edge-upper` vertices = (1085,802) and (1110,808).

At x=1105:
- t = (1105-1085)/(1110-1085) = 20/25 = 0.8 exactly;
- edge y = 802 + 0.8*(808-802) = 806.8 px exactly;
- clearance = 806.8 - 704 = **102.8 px exactly**.

Frozen minimum: **100 px**.
Exact surplus: **2.8 px**.
Result: **PASS**.

Linear interpolation is the definition of the SVG polyline segment. No decimal reconstruction of normalized coordinates is needed or preferred.

MACHINE-READABLE CONSEQUENCE

The active Stage 6 scaffold manifest and SVG embedded metadata must state 102.8 px rather than 120 px. The permanent verifier must derive the controlling Stage 6 metric from the integer SVG and treat the four-decimal normalized manifest as a projection, not a higher-precision authority.

The Stage 6M physical reconstruction remains a separate normalized representation with approximately 102.854839694656 px clearance; that difference is representation quantization, not a contradiction.

SCOPE / PRESERVATION

No visible Stage 6 geometry changes.
No Stage 6M manifest/report or sanitized renderer reference changes.
No sterile renderer packet changes.
No 100 px floor changes.
No post-render score or stop condition changes.
Round 08 remains historically spent and failed exactly as recorded in its canonical evaluation.

Where earlier C08 evidence says `120 px` is the current clearance, read it only as obsolete undeformed-baseline planning provenance. Where the first correction says `102.8395582822086 px` is exact, that arithmetic result is superseded by 102.8 px.

DISPOSITION

Exact controlling Stage 6 changed-state clearance: **102.8 px**.
Frozen minimum: **100 px**.
Result: **PASS**.

This arithmetic correction neither rescues nor alters the failed Round 08 artwork. It only restores consistency between exact source geometry and active metadata/tooling.
