ENSEMBLE WEBSITE / KYMAEAN — PUBLICATION SOURCE BOUNDARY

Status: D-R1 PHASE 4 STRUCTURAL BOUNDARY / NO PRODUCTION SITE SELECTED
Created: 2026-09-07

PURPOSE
This directory is reserved as the repository publication source for the Kymaean public website.

CURRENT LAW
- `site/` is publication source only; it is not a design-research workspace.
- `prototypes/` remains experimental/renderable research evidence and is not publication source by implication.
- `assets/` remains repository asset storage and is not moved by D-R1 Phase 4.
- The approved Website V0 Site Spine narrative order is design law, but its approval explicitly did not select a final website or production implementation.
- Therefore no historical prototype is copied, moved, renamed or promoted into `site/` merely to populate this directory.

DEPLOYMENT GUARD
There is intentionally no `index.html` in `site/` at this checkpoint.

Do not point the production Cloudflare publication root at `site/` until an explicitly authorized deployable website source exists here and its required assets/paths have been recursively audited.

Do not point Cloudflare at `prototypes/` as a substitute.

NEXT
Resolve the Phase 4 deployable-source eligibility/migration question from current ACTIVE LAW and current hero/website boundaries. If no production website source is yet authorized, preserve this empty publication boundary and keep deployment configuration unchanged rather than promoting historical evidence.
