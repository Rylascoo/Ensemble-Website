<!-- D-R1-STATUS: HISTORICAL EVIDENCE -->

# Website V2.2 Mascot Placement Study

Non-production deterministic composition study opened by Design Ledger L-188.

The prototype reuses the exact current production Stage and wordmark markup/CSS. `site/public/**` is not modified.

Variants:
- `?variant=control` — current production composition without Bellweather.
- `?variant=a1` — distant standing presence.
- `?variant=a2` — distant crossing/departing placement study.
- `?variant=a3` — partially occluded threshold study.
- `?variant=a4` — delayed reveal using the A1 placement.

Bellweather overlay is a derived background-isolated study asset from foundational Drive reference `1nDt7EdFHeq35DcavAlUopAQI47rf8WMx`. Canonical derived copy is Drive `1yQgUlxkykvUA4AMwMSDe5xYrjs0A6l_z`. It is not a new mascot master and may not replace L-187 authority.

The overlay exists only to test scale, position, hierarchy and reveal timing. Pose/orientation quality is not authoritative; a later mascot-specific successor may replace the overlay while preserving adopted composition law.
