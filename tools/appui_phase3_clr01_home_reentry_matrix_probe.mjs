import {spawn} from 'node:child_process'; import fs from 'node:fs'; import path from 'node:path';
const [outArg,profileArg,portArg]=process.argv.slice(2),out=path.resolve(outArg),profile=path.resolve(profileArg),port=Number(portArg||9571),browser=process.env.BROWSER;if(!browser)throw new Error('BROWSER not set');fs.rmSync(out,{recursive:true,force:true});fs.mkdirSync(out,{recursive:true});fs.rmSync(profile,{recursive:true,force:true});fs.mkdirSync(profile,{recursive:true});
const html=fs.readFileSync(path.resolve('prototypes/appui-01/phase3-clr01-home-contextual-reentry-comparison-01.html'),'utf8');const cp=spawn(browser,['--headless=new','--no-sandbox','--disable-gpu','--disable-dev-shm-usage',`--remote-debugging-port=${port}`,`--user-data-dir=${profile}`,'about:blank'],{stdio:['ignore','ignore','inherit']});const sleep=ms=>new Promise(r=>setTimeout(r,ms));async function wait(){for(let i=0;i<300;i++){try{let r=await fetch(`http://127.0.0.1:${port}/json/version`);if(r.ok)return}catch{}await sleep(100)}throw new Error('CDP unavailable')}
try{await wait();let r=await fetch(`http://127.0.0.1:${port}/json/new?about%3Ablank`,{method:'PUT'}),t=await r.json();const ws=new WebSocket(t.webSocketDebuggerUrl);await new Promise((a,b)=>{ws.onopen=a;ws.onerror=b});let id=0;const pend=new Map();ws.onmessage=e=>{let m=JSON.parse(e.data);if(m.id&&pend.has(m.id)){let q=pend.get(m.id);pend.delete(m.id);m.error?q.b(new Error(JSON.stringify(m.error))):q.a(m.result)}};const send=(method,params={})=>new Promise((a,b)=>{let n=++id;pend.set(n,{a,b});ws.send(JSON.stringify({id:n,method,params}))});await send('Page.enable');await send('Runtime.enable');const frame=(await send('Page.getFrameTree')).frameTree.frame.id;
const cases=[['normal',1600],['normal',1024],['normal',390],['normal',320],['forced',320],['text200',320],['spacing',320]],results=[];
for(const [mode,width] of cases){await send('Emulation.setDeviceMetricsOverride',{width,height:2600,deviceScaleFactor:1,mobile:false,screenWidth:width,screenHeight:2600});await send('Emulation.setEmulatedMedia',{media:'screen',features:mode==='forced'?[{name:'forced-colors',value:'active'}]:[{name:'forced-colors',value:'none'}]});await send('Page.setDocumentContent',{frameId:frame,html});await sleep(100);if(mode==='text200')await send('Runtime.evaluate',{expression:`document.documentElement.style.fontSize='200%'`});if(mode==='spacing')await send('Runtime.evaluate',{expression:`(()=>{const s=document.createElement('style');s.textContent='p,li,button,.role-support,.role-body,.role-eyebrow,.link,.mini-prod,.resume-status div{line-height:1.5!important;letter-spacing:.12em!important;word-spacing:.16em!important}';document.head.appendChild(s)})()`});await sleep(90);
const expr=`(()=>{const all=[...document.querySelectorAll('*')];const over=all.map((e,i)=>{let r=e.getBoundingClientRect();return{i,tag:e.tagName,cls:typeof e.className==='string'?e.className:'',text:(e.textContent||'').trim().replace(/\\s+/g,' ').slice(0,80),left:r.left,right:r.right,scrollWidth:e.scrollWidth,clientWidth:e.clientWidth}}).filter(x=>x.right>innerWidth+.5||x.left<-.5||x.scrollWidth>x.clientWidth+1).slice(0,100);const apps=[...document.querySelectorAll('.app[class*=clr-]')].map(e=>({id:[...e.classList].find(x=>x.startsWith('clr-')),ui:getComputedStyle(e).getPropertyValue('--ui').trim(),text:getComputedStyle(e).getPropertyValue('--text').trim(),sub:getComputedStyle(e).getPropertyValue('--sub').trim(),line:getComputedStyle(e).getPropertyValue('--line').trim(),field:getComputedStyle(e).getPropertyValue('--field').trim(),cut:getComputedStyle(e).getPropertyValue('--cut').trim()}));const mats=[...document.querySelectorAll('.material-field')].map(e=>({before:getComputedStyle(e,'::before').content,beforeW:getComputedStyle(e,'::before').width,after:getComputedStyle(e,'::after').content,afterW:getComputedStyle(e,'::after').width}));return{mode:${JSON.stringify(mode)},forced:matchMedia('(forced-colors: active)').matches,viewport:{width:innerWidth,scrollWidth:document.documentElement.scrollWidth,scrollHeight:document.documentElement.scrollHeight},overflowElements:over,apps,appCount:apps.length,materialFieldCount:mats.length,materialPseudos:mats,selected:document.querySelectorAll('.rail button.selected').length,focus:document.querySelectorAll('.primary.focus').length,images:document.images.length,external:[...document.querySelectorAll('link[href],script[src],img[src]')].map(x=>x.href||x.src)};})()`;const ev=await send('Runtime.evaluate',{expression:expr,returnByValue:true});let d=ev.result.value;results.push(d);fs.writeFileSync(path.join(out,`clr-${mode}-${width}.json`),JSON.stringify(d,null,2)+'\n');if(mode==='normal'&&(width===1600||width===390)){let lm=await send('Page.getLayoutMetrics'),cs=lm.cssContentSize||lm.contentSize,shot=await send('Page.captureScreenshot',{format:'png',captureBeyondViewport:true,clip:{x:0,y:0,width:Math.ceil(cs.width),height:Math.ceil(cs.height),scale:1}});fs.writeFileSync(path.join(out,`clr-${mode}-${width}.png`),Buffer.from(shot.data,'base64'))}}
fs.writeFileSync(path.join(out,'matrix.json'),JSON.stringify(results,null,2)+'\n');console.log(results.map(x=>`${x.mode} ${x.viewport.width} apps=${x.appCount} material=${x.materialFieldCount} overflow=${x.viewport.scrollWidth>x.viewport.width} elements=${x.overflowElements.length} forced=${x.forced}`).join('\n'));try{ws.send(JSON.stringify({id:++id,method:'Browser.close'}));await sleep(100)}catch{}ws.close()}finally{if(cp.exitCode===null)cp.kill('SIGTERM');await Promise.race([new Promise(r=>cp.once('exit',r)),sleep(1000)])}
