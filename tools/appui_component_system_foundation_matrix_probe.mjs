import {spawn} from 'node:child_process'; import fs from 'node:fs'; import path from 'node:path';
const [outDirArg,profileArg,portArg]=process.argv.slice(2);const out=path.resolve(outDirArg),profile=path.resolve(profileArg),port=Number(portArg||9567),browser=process.env.BROWSER;if(!browser)throw new Error('BROWSER not set');fs.rmSync(out,{recursive:true,force:true});fs.mkdirSync(out,{recursive:true});fs.rmSync(profile,{recursive:true,force:true});fs.mkdirSync(profile,{recursive:true});const html=fs.readFileSync(path.resolve('prototypes/appui-01/component-system-foundation-01.html'),'utf8');const cp=spawn(browser,['--headless=new','--no-sandbox','--disable-gpu','--disable-dev-shm-usage',`--remote-debugging-port=${port}`,`--user-data-dir=${profile}`,'about:blank'],{stdio:['ignore','ignore','inherit']});const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function waitPort(){for(let i=0;i<300;i++){try{const r=await fetch(`http://127.0.0.1:${port}/json/version`);if(r.ok)return}catch{}await sleep(100)}throw new Error('CDP unavailable')}
try{await waitPort();const r=await fetch(`http://127.0.0.1:${port}/json/new?about%3Ablank`,{method:'PUT'});const target=await r.json();const ws=new WebSocket(target.webSocketDebuggerUrl);await new Promise((res,rej)=>{ws.onopen=res;ws.onerror=rej});let id=0;const pending=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&pending.has(m.id)){const q=pending.get(m.id);pending.delete(m.id);m.error?q.rej(new Error(JSON.stringify(m.error))):q.res(m.result)}};const send=(method,params={})=>new Promise((res,rej)=>{const n=++id;pending.set(n,{res,rej});ws.send(JSON.stringify({id:n,method,params}))});await send('Page.enable');await send('Runtime.enable');const frameId=(await send('Page.getFrameTree')).frameTree.frame.id;
const cases=[['normal',1600],['normal',1024],['normal',390],['normal',320],['forced',320],['text200',320],['spacing',320]],results=[];
for(const [mode,width] of cases){await send('Emulation.setDeviceMetricsOverride',{width,height:3200,deviceScaleFactor:1,mobile:false,screenWidth:width,screenHeight:3200});await send('Emulation.setEmulatedMedia',{media:'screen',features:mode==='forced'?[{name:'forced-colors',value:'active'}]:[{name:'forced-colors',value:'none'}]});await send('Page.setDocumentContent',{frameId,html});await sleep(100);if(mode==='text200')await send('Runtime.evaluate',{expression:`document.documentElement.style.fontSize='200%'`});if(mode==='spacing')await send('Runtime.evaluate',{expression:`(()=>{const s=document.createElement('style');s.textContent='.lede,.label,.fieldlabel,.help,.message,.choice,.switch,.row,.notice,.dialog,.law,summary,details p{line-height:1.5!important;letter-spacing:.12em!important;word-spacing:.16em!important}';document.head.appendChild(s)})()`});await sleep(80);await send('Input.dispatchKeyEvent',{type:'keyDown',key:'Tab',code:'Tab',windowsVirtualKeyCode:9});await send('Input.dispatchKeyEvent',{type:'keyUp',key:'Tab',code:'Tab',windowsVirtualKeyCode:9});await sleep(40);
const expr=`(()=>{const all=[...document.querySelectorAll('*')],over=all.map((e,i)=>{const r=e.getBoundingClientRect();return{i,tag:e.tagName,cls:typeof e.className==='string'?e.className:'',text:(e.textContent||'').trim().replace(/\\s+/g,' ').slice(0,80),left:r.left,right:r.right,sw:e.scrollWidth,cw:e.clientWidth}}).filter(x=>x.right>innerWidth+.5||x.left<-.5||((x.sw>x.cw+1)&&!['INPUT','TEXTAREA','SELECT'].includes(x.tag))).slice(0,100);const named=e=>(e.getAttribute('aria-label')||e.textContent||'').trim();const active=document.activeElement,acs=active?getComputedStyle(active):null;return{mode:${JSON.stringify(mode)},forcedColors:matchMedia('(forced-colors: active)').matches,viewport:{width:innerWidth,scrollWidth:document.documentElement.scrollWidth,scrollHeight:document.documentElement.scrollHeight},overflowElements:over,themeCount:document.querySelectorAll('.theme').length,buttonCount:document.querySelectorAll('button').length,missingButtonNames:[...document.querySelectorAll('button')].filter(b=>!named(b)).length,undersizedIconButtons:[...document.querySelectorAll('.iconbtn')].filter(b=>{const r=b.getBoundingClientRect();return r.width<31.5||r.height<31.5}).length,textInputCount:document.querySelectorAll('.input').length,undersizedTextInputs:[...document.querySelectorAll('input.input')].filter(e=>e.getBoundingClientRect().height<31.5).length,choiceCount:document.querySelectorAll('.choice,.switch').length,rowCount:document.querySelectorAll('.row').length,selectedWitnesses:document.querySelectorAll('.selecteddemo').length,focusWitnesses:document.querySelectorAll('.focusdemo').length,noticeCount:document.querySelectorAll('.notice').length,dialogCount:document.querySelectorAll('.dialog').length,menuCount:document.querySelectorAll('.menu').length,fluentFont:document.fonts.check('16px "Segoe Fluent Icons"'),focusOutline:acs?{style:acs.outlineStyle,width:acs.outlineWidth,offset:acs.outlineOffset}:null,external:[...document.querySelectorAll('link[href],script[src],img[src]')].map(x=>x.href||x.src)};})()`;const ev=await send('Runtime.evaluate',{expression:expr,returnByValue:true});if(ev.exceptionDetails)throw new Error(JSON.stringify(ev.exceptionDetails));const d=ev.result.value;results.push(d);fs.writeFileSync(path.join(out,`component-foundation-${mode}-${width}.json`),JSON.stringify(d,null,2)+'\n');if(mode==='normal'&&(width===1600||width===390)){const l=await send('Page.getLayoutMetrics'),cs=l.cssContentSize||l.contentSize,shot=await send('Page.captureScreenshot',{format:'png',captureBeyondViewport:true,clip:{x:0,y:0,width:Math.ceil(cs.width),height:Math.ceil(cs.height),scale:1}});fs.writeFileSync(path.join(out,`component-foundation-${mode}-${width}.png`),Buffer.from(shot.data,'base64'))}}
fs.writeFileSync(path.join(out,'matrix.json'),JSON.stringify(results,null,2)+'\n');console.log(results.map(x=>`${x.mode} ${x.viewport.width} overflow=${x.viewport.scrollWidth>x.viewport.width}/${x.overflowElements.length} names=${x.missingButtonNames} icons=${x.undersizedIconButtons} inputs=${x.undersizedTextInputs} focus=${x.focusOutline?.style}/${x.focusOutline?.width}/${x.focusOutline?.offset}`).join('\n'));try{ws.send(JSON.stringify({id:++id,method:'Browser.close'}));await sleep(120)}catch{}ws.close();}finally{if(cp.exitCode===null)cp.kill('SIGTERM');await Promise.race([new Promise(r=>cp.once('exit',r)),sleep(1200)])}
