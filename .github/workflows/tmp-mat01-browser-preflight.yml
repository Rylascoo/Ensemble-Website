name: Temporary MAT-01 browser preflight

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'MAT-01 browser preflight paused during repaired-packet reconciliation.'
