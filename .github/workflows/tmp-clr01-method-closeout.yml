name: Temporary CLR-01 method closeout

on:
  push:
    branches:
      - design/clr01-method

permissions:
  contents: write

jobs:
  closeout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: design/clr01-method
      - name: Correct F4, update handoff, append ledger, remove helper
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from pathlib import Path

          method = Path('docs/evidence/CLR_01_CROSS_SURFACE_COLOR_LOGIC_INDEPENDENCE_METHOD_AND_MATRIX_01.json')
          data = json.loads(method.read_text())
          data['families']['F4']['APP']['boundary'] = '#6F7F99'
          data['families']['F4']['APP']['soft_surface'] = '#E7EBF3'
          data['families']['F4']['WEB']['boundary'] = '#687DA5'
          data['families']['F4']['WEB']['soft_surface'] = '#E1E7F2'
          method.write_text(json.dumps(data, indent=2, ensure_ascii=False) + '\n')

          Path('CURRENT_STATE.md').write_text('''<!-- D-R1-STATUS: ACTIVE LAW -->

# Ensemble Website — Current State

Updated: 2026-09-09

## Authority
Design/brand: `Rylascoo/Ensemble-Website`. Engineering/product truth: `Rylascoo/Ensemble-Project/CURRENT_STATE.md`. Workflow: `AGENTS.md` + `docs/KYMAEAN_VISUAL_DESIGN_WORKFLOW_AUTHORITY_01.md`. Durable closure: `docs/evidence/DESIGN_LEDGER.md`.

## Repository / cross-project
`main` is authoritative. `site/` alone is deployable/public. Central sequencing: `Rylascoo/Ensemble-Project/docs/PROJECT_EXECUTION_QUEUE.md`.

## Hero — STATIC RESEARCH PAUSED
Frozen Hero Brief 02/Falsification 01/Surface Escape/Harness/ODR-33 unchanged. No final hero. Q-DESIGN-02 Stage motion still waits on real blinded E0-A vs E0-E evidence.

## Shared brand — CLR-01 METHOD FROZEN
MOT-01 is closed. Provisional motion evidence only: **F1A Tight** = bounded within-context succession; **CS2 Balanced Crossfade Exchange** = eligible already-ready genuine context replacement; **CUT** = first-class semantic/accessibility/fatigue outcome. No production motion tokens.

Q-DESIGN-17 selected **CLR-01 — Cross-Surface Color-Logic Independence**. Q-DESIGN-18 method is frozen in `docs/evidence/CLR_01_CROSS_SURFACE_COLOR_LOGIC_INDEPENDENCE_METHOD_AND_MATRIX_01.json`.

CLR-01 tests 24 deterministic cells: CNEG grayscale + CCONV conventional-blue calibration + F1 Split Temperature + F2 Monochrome/Intruder + F3 Complementary Poles + F4 Chromatic Ladder × APP/WEB × WIDE/320. All hex values are experiment fixtures only. Mineral Theater and historical palette incumbents are quarantined.

Hard floors precede judgment: normal text 4.5:1, required non-text/boundaries 3:1, visible non-color-only focus, 320 reflow, no color-only meaning, forced-colors comprehension, flat-solid source isolation and no post-result retune. Direct Design Sol + Director review follows only after mechanical preflight.

## Exact next action / hard boundary
Q-DESIGN-19: materialize the exact 24-cell browser packet + manifest from the frozen CLR-01 method; run deterministic contrast/isolation/320/forced-colors preflight; freeze Design Sol view before Director preference.

No image generation/renderer spend. No production color/motion tokens, app/site theming, semantic status colors, incumbent redesign, Stage motion, final identity or Phase-C convergence.
''')
          PY
          cat >> docs/evidence/DESIGN_LEDGER.md <<'EOF'

          ## L-054 — CLR-01 cross-surface color-logic method frozen

          **State:** Q-DESIGN-18 DONE / CLR-01 METHOD FROZEN / Q-DESIGN-19 MATERIALIZATION NEXT

          `docs/evidence/CLR_01_CROSS_SURFACE_COLOR_LOGIC_INDEPENDENCE_METHOD_AND_MATRIX_01.json` freezes a non-render 24-cell APP/WEB × WIDE/320 experiment that tests color-role logic independently of symbol, wordmark, bespoke type, material/texture, imagery, Quiet Stage, motion and semantic status colors. The carrier and content are identical across families; all candidate values are experiment fixtures only.

          Matrix: CNEG grayscale; CCONV conventional product-blue genericity calibration; F1 Split Temperature; F2 Monochrome + Intruder; F3 Complementary Poles; F4 Chromatic Ladder. APP/WEB may differ only by the exact frozen values while preserving each family's hue topology, role allocation and emphasis order. A recursive pass corrected F4 before adoption so its blue ladder actually spans soft surface → boundary → concentrated accent rather than being described as chromatic while using neutral structural roles.

          Accessibility/isolation floors are frozen before execution: normal text >=4.5:1; required non-text/boundaries >=3:1; visible non-color-only focus; 320px reflow; no color-only meaning; forced-colors/high-contrast comprehension; no gradients/images/material effects/motion identity carriers; historical palette quarantine after source freeze; no retune or replacement family after result exposure. Zero survivors remains valid.

          **Next gate:** Q-DESIGN-19 materializes the deterministic packet/manifest, preflights all 24 cells, and freezes Design Sol's independent view before Director preference.

          **Guardrail:** no final palette, production color tokens, theme implementation, semantic status-color system, image generation/renderer spend, motion retune, Stage motion, final identity or Phase-C convergence.

          **APPROVED BY STANDING DIRECTOR DELEGATION — CLEAN RECURSIVE AUDIT.**
          EOF
          git rm .github/workflows/tmp-clr01-method-closeout.yml
          git add CURRENT_STATE.md docs/evidence/CLR_01_CROSS_SURFACE_COLOR_LOGIC_INDEPENDENCE_METHOD_AND_MATRIX_01.json docs/evidence/DESIGN_LEDGER.md
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Freeze CLR-01 method and advance to materialization'
          git push origin HEAD:design/clr01-method
